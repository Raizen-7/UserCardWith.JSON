import './App.css';
import Bulb from './Componets/Bulb';
import {useState} from 'react';
import users from './users.json';

function App() {
  const [ isOn, setIsOn ] = useState(false)

  const toggleLight = () => {
    setIsOn(!isOn)
  }
  const user = users[0].name.first;
  console.log(users[0].name.last);
  console.log(user);
  return (
    <div className='App'>
      <Bulb isOn={isOn} toggleLight={toggleLight} />
      <Bulb isOn={isOn} toggleLight={toggleLight} />
      <Bulb isOn={isOn} toggleLight={toggleLight} />
      <Bulb isOn={isOn} toggleLight={toggleLight} />
    </div>
  );
};

export default App;